{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && printf '%s\\n' '_worker.js' > dist/.assetsignore && node scripts/fix-asset-urls.js && nohup node scripts/watch-and-upload.js > watch-and-upload.log 2>&1 &",
    "build:deploy": "npm run build && ./deploy-site.sh",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.4",
    "@astrojs/mdx": "^5.0.6",
    "@astrojs/sitemap": "^3.7.2",
    "@aws-sdk/client-s3": "^3.879.0",
    "astro": "^6.3.7",
    "aws-sdk": "^2.1692.0",
    "chokidar": "^4.0.3",
    "dotenv": "^16.5.0",
    "nodemon": "^3.1.10"
  }
}
