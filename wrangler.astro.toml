name = "kdzu-org-astro-site"
compatibility_flags = ["nodejs_compat"]
account_id = "38b2250cde779e4a92c63d139be80a9c"
compatibility_date = "2024-09-23"

# Intentionally no `main` — @astrojs/cloudflare sets the dev entrypoint.
# Production deploy uses wrangler-astro.toml after `astro build`.

[observability.logs]
enabled = true
